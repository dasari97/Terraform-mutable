pipeline {
    agent any
    
    stages {
        
        
        stage ('Terraform Apply') {
            steps {
                sh 'cd databases; make dev'
                sh 'echo Hello'
            }
            
        }
    }

post {
    always {
      cleanWs()
    }
  }
}