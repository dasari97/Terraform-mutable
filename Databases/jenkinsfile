pipeline {
    agent any
    
    stages {
        
        
        stage ('Terraform Apply') {
            steps {
                sh 'echo Hello'
                sh 'echo Hello'
            }
            
        }
    }

post {
    always {
      cleanWs()
    }
  }
}