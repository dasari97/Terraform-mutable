pipeline {
    agent any
    
    stages {
        
        
        stage ('Terraform Apply') {
            steps {
                sh 'cd Databsaes; make dev'
            }
            
        }
    }

post {
    always {
      cleanWs disableDeferredWipeout: true
    }
  }
}