pipeline {
    agent any
    
    stages {
        
        
        stage ('Terraform Apply') {
            steps {
                sh 'echo Hello'
                sh 'cd VPC; make dev'
                sh 'echo Hello'
            }
            
        }
    }

post {
    always {
      cleanWs disableDeferredWipeout: true
    }
  }
}