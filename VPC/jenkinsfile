pipeline {
    agent any
    
    stages {
        
        
        stage ('Terraform Apply') {
            steps {
                sh 'echo Hello'
                sh 'cd VPC; make dev'
            }
            
        }
    }

post {
    always {
      cleanWs()
    }
  }
}