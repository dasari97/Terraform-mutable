pipeline {
    agent any
    
    stages {
        
        
        stage ('Terraform Apply') {
            steps {
                sh 'cd vpc; make dev'
                sh 'echo Hello'
            }
            
        }
    }

post {
    always {
      cleanWs()
    }
  }
}
    
}